# Frontend Environment Variables
# Copy this file to .env and fill in your actual values

# API Configuration
# For local development, use proxy (/api)
# For production, use your actual backend URL
VITE_API_URL=/api
VITE_API_BASE_URL=/api

# Gemini AI (Optional - can be handled by backend)
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# ⚠️ IMPORTANT NOTES:
# 1. Never commit your actual .env file to Git
# 2. VITE_ prefix is required for Vite to expose variables to the client
# 3. Don't put sensitive secrets here - they will be exposed in the browser
# 4. For production, set these in Vercel Environment Variables
