# 📝 تحديث Vercel Environment Variables - خطوة بخطوة

## 🎯 المتغيرات المطلوبة

### 1. VITE_API_URL ⭐ (الأهم)
**القيمة الحالية:** (تحتاج تحديث)  
**القيمة الجديدة:**
```
https://tsunamic-unshameable-maricruz.ngrok-free.dev/api
```

**الخطوات:**
1. اضغط على النقاط الثلاث (...) بجانب `VITE_API_URL`
2. اختر **Edit**
3. استبدل القيمة بالرابط أعلاه
4. تأكد من اختيار **All Environments** (Production, Preview, Development)
5. اضغط **Save**

---

### 2. VITE_API_BASE_URL ⭐ (مطلوب إضافته)
**الحالة:** غير موجود - يجب إضافته  
**القيمة:**
```
https://tsunamic-unshameable-maricruz.ngrok-free.dev/api
```

**الخطوات:**
1. اضغط على **Add New** أو **Add Variable**
2. Name: `VITE_API_BASE_URL`
3. Value: الرابط أعلاه
4. اختر **All Environments**
5. اضغط **Save**

---

### 3. VITE_GEMINI_API_KEY (اختياري - للـ Frontend)
**الحالة:** غير موجود  
**القيمة:**
```
AIzaSyCPyrQRyVVJNf23GeNMyZ8u9dpFY-TZyto
```

**الخطوات:**
1. اضغط **Add New**
2. Name: `VITE_GEMINI_API_KEY`
3. Value: المفتاح أعلاه
4. اختر **All Environments**
5. اضغط **Save**

---

## ⚠️ المتغيرات الموجودة (لا تحتاج تغيير)

### DATABASE_URL
- ✅ **اتركها كما هي** - هذه للـ Backend فقط
- Vercel لا يحتاجها للـ Frontend

### NODE_ENV
- ✅ **اتركها كما هي** - Vercel يديرها تلقائياً

### DIRECT_URL
- ✅ **اتركها كما هي** - للـ Backend فقط

### JWT_SECRET
- ✅ **اتركها كما هي** - للـ Backend فقط

---

## 📸 الشكل النهائي المطلوب

بعد التحديث، يجب أن تكون المتغيرات كالتالي:

```
✅ DATABASE_URL          (موجود - لا تغيير)
✅ NODE_ENV              (موجود - لا تغيير)
✅ DIRECT_URL            (موجود - لا تغيير)
✅ JWT_SECRET            (موجود - لا تغيير)
✅ VITE_API_URL          (محدّث - الرابط الجديد)
✅ VITE_API_BASE_URL     (جديد - نفس الرابط)
✅ VITE_GEMINI_API_KEY   (جديد - اختياري)
```

---

## 🔄 بعد التحديث

### لا حاجة لعمل Redeploy!
التغييرات في Environment Variables تسري فوراً على:
- ✅ Production
- ✅ Preview Deployments
- ✅ Development

### اختبار الاتصال
1. افتح موقعك على Vercel
2. افتح Developer Tools (F12)
3. اذهب إلى Network Tab
4. حاول تسجيل الدخول أو تحميل البيانات
5. تحقق من أن الطلبات تذهب إلى رابط Ngrok الجديد

---

## 🎯 Checklist

- [ ] تحديث `VITE_API_URL` بالرابط الجديد
- [ ] إضافة `VITE_API_BASE_URL` بنفس الرابط
- [ ] إضافة `VITE_GEMINI_API_KEY` (اختياري)
- [ ] التأكد من اختيار **All Environments** لكل متغير
- [ ] حفظ التغييرات
- [ ] اختبار الموقع على Vercel

---

## 🚨 ملاحظات مهمة

### 1. رابط Ngrok سيتغير
عند إعادة تشغيل Ngrok، ستحتاج لتكرار هذه الخطوات مع الرابط الجديد.

### 2. الحل الدائم
للحصول على رابط ثابت، استخدم:
- **Ngrok Pro** ($8/شهر) - Static Domain
- **VPS** (DigitalOcean, AWS, etc.) - موصى به للإنتاج

### 3. الأمان
- ❌ لا ترفع `.env` على Git
- ✅ استخدم `.env.example` فقط
- ✅ غيّر الـ Secrets في Production

---

## 📞 إذا واجهت مشاكل

### CORS Error
تأكد من أن `server-nestjs/src/main.ts` يحتوي على روابط Vercel في CORS:
```typescript
origin: [
  'https://hakeem-jordan-jordan.vercel.app',
  'https://hakeemjordanjo.vercel.app',
  // أضف روابط Vercel الأخرى
]
```

### 404 Not Found
تأكد من:
- ✅ Ngrok يعمل (`http://127.0.0.1:4040`)
- ✅ Backend يعمل (`http://localhost:3000/api`)
- ✅ الرابط صحيح مع `/api` في النهاية

### Environment Variables لا تعمل
- ✅ تأكد من البادئة `VITE_`
- ✅ تأكد من اختيار **All Environments**
- ✅ انتظر دقيقة واحدة بعد الحفظ

---

**آخر تحديث:** 19 يناير 2026 - 11:58 AM  
**Ngrok URL الحالي:** `https://tsunamic-unshameable-maricruz.ngrok-free.dev`  
**حالة النظام:** ✅ Backend يعمل | ✅ Frontend يعمل | ✅ Ngrok يعمل
